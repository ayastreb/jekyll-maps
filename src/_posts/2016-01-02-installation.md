---
layout: post
title: Installation
anchor: installation
---

1.&nbsp;Add the following to your site's `Gemfile`:

  ```ruby
  gem 'jekyll-maps'
  ```
2.&nbsp;Add the following to your site's `_config.yml`:

  ```yaml
  gems:
    - jekyll-maps
  ```
