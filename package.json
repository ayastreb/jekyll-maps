{
  "private": true,
  "devDependencies": {
    "grunt": "^1.0.1",
    "grunt-build-control": "^0.7.0",
    "grunt-jekyll": "^0.4.4",
    "jit-grunt": "^0.10.0",
    "time-grunt": "^1.3.0"
  },
  "engines": {
    "node": ">=0.10.0"
  },
  "scripts": {
    "test": "grunt test"
  },
  "eslintConfig": {
    "env": {
      "node": true,
      "browser": true,
      "mocha": true
    },
    "rules": {
      "quotes": [
        2,
        "single"
      ]
    }
  }
}
